import * as React from 'react';
import { Box } from '@mui/material';
import OrdersTabWrapper from 'src/layout/Orders/OrdersTabWrapper';

export default function GetOrder() {
    return (
        <Box>
            <OrdersTabWrapper />
        </Box>
    );
}
